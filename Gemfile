# Gemfile

source 'https://rubygems.org' # This is where Bundler will fetch gems from.

# Use the Jekyll gem to build your site.
gem 'jekyll', '~> 4.0'

# Optional: Use the `jekyll-assets` gem for asset management (CSS/JS).
gem 'jekyll-assets'

# gem versions track against tailwind releases
# gem "tailwindcss-ruby", "~> 3.4.17"

# Optional: Use `jekyll-asset-minifier` for asset minification.
gem 'jekyll-minifier'
gem 'jekyll-sitemap'
gem 'jekyll-seo-tag'
gem 'httpparty'
gem 'jekyll-paginate'
gem 'nokogiri'
gem 'jekyll-purge-css', :git => "https://github.com/MUmarShahbaz/jekyll-purge-css.git"
group :jekyll_plugins do
 # ...
 gem "jekyll-tailwindcss"
end



# If you're using any CSS preprocessors (like Sass), you can add this:
# gem 'sassc'

# Add any other gems your project may need.
